@model IEnumerable<ControleDeEstoque.ViewModels.TipoPagamentoViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Formas de Pagamento</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoPagamentoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoPagamentoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoPagamentoId })
        </td>
    </tr>
}

</table>
